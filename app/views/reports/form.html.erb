<%= form_for @report do |r| %>

  <%= r.label :user_name, 'Nom de l'utilisateur'%><br>
  <%= r.text_field :user_name %>

  
  <%= r.label :forest_name, 'Nom de la forêt%><br>
  <%= r.text_field :forest_name %>


  <%= r.label :observation, 'Rapport'%><br>
  <%= r.text_area :observation %>

  <%= r.submit 'Envoyer' %>
 
<% end %>